{
  "name": "eslint",
  "version": "1.0.0",
  "description": "test eslint",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --ext .vue,.js testCode.js -s",
    "fix": "eslint --ext .vue,.js testCode.js --fix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-eslint": "^10.1.0",
    "eslint": "^6.8.0",
    "eslint-plugin-vue": "^6.2.2"
  }
}
